{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn" style="background-color: #343A40; color: white; margin-bottom: 20px; margin-top: 20px">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
